# Generated by cloudcoil-model-codegen v0.7.1
# DO NOT EDIT

from typing import (
    Annotated,
    Any,
    Callable,
    ClassVar,
    Literal,
    overload,
)

from cloudcoil.apimachinery import ObjectMeta
from cloudcoil.pydantic import (
    UNSET,
    BaseModel,
    BaseModelBuilder,
    BuilderContextBase,
    GenericListBuilder,
    ListBuilderContext,
    Never,
    Self,
)
from cloudcoil.resources import Resource
from pydantic import AwareDatetime, Field


class KustomizationSpecCommonMetadata(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecCommonMetadata"]:
            return KustomizationSpecCommonMetadata

        def build(self) -> "KustomizationSpecCommonMetadata":
            return KustomizationSpecCommonMetadata(**self._attrs)

        def annotations(self, value: dict[str, str] | None, /) -> Self:
            "Annotations to be added to the object\u0027s metadata."
            return self._set("annotations", value)

        def labels(self, value: dict[str, str] | None, /) -> Self:
            "Labels to be added to the object\u0027s metadata."
            return self._set("labels", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecCommonMetadata.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecCommonMetadata.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecCommonMetadata."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationSpecCommonMetadata", Builder]:
        return GenericListBuilder[
            KustomizationSpecCommonMetadata, KustomizationSpecCommonMetadata.Builder
        ]()

    annotations: dict[str, str] | None = None
    "Annotations to be added to the object\u0027s metadata."
    labels: dict[str, str] | None = None
    "Labels to be added to the object\u0027s metadata."


class KustomizationSpecDecryptionSecretRef(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecDecryptionSecretRef"]:
            return KustomizationSpecDecryptionSecretRef

        def build(self) -> "KustomizationSpecDecryptionSecretRef":
            return KustomizationSpecDecryptionSecretRef(**self._attrs)

        def name(self, value: str, /) -> Self:
            "Name of the referent."
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecDecryptionSecretRef.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecDecryptionSecretRef.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecDecryptionSecretRef."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationSpecDecryptionSecretRef", Builder]:
        return GenericListBuilder[
            KustomizationSpecDecryptionSecretRef,
            KustomizationSpecDecryptionSecretRef.Builder,
        ]()

    name: str
    "Name of the referent."


class KustomizationSpecDependsOnItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecDependsOnItem"]:
            return KustomizationSpecDependsOnItem

        def build(self) -> "KustomizationSpecDependsOnItem":
            return KustomizationSpecDependsOnItem(**self._attrs)

        def name(self, value: str, /) -> Self:
            "Name of the referent."
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "Namespace of the referent, when not specified it acts as LocalObjectReference."
            return self._set("namespace", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecDependsOnItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecDependsOnItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecDependsOnItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationSpecDependsOnItem", Builder]:
        return GenericListBuilder[
            KustomizationSpecDependsOnItem, KustomizationSpecDependsOnItem.Builder
        ]()

    name: str
    "Name of the referent."
    namespace: str | None = None
    "Namespace of the referent, when not specified it acts as LocalObjectReference."


class KustomizationSpecHealthChecksItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecHealthChecksItem"]:
            return KustomizationSpecHealthChecksItem

        def build(self) -> "KustomizationSpecHealthChecksItem":
            return KustomizationSpecHealthChecksItem(**self._attrs)

        def api_version(self, value: str | None, /) -> Self:
            "API version of the referent, if not specified the Kubernetes preferred version will be used."
            return self._set("api_version", value)

        def kind(self, value: str, /) -> Self:
            "Kind of the referent."
            return self._set("kind", value)

        def name(self, value: str, /) -> Self:
            "Name of the referent."
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "Namespace of the referent, when not specified it acts as LocalObjectReference."
            return self._set("namespace", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecHealthChecksItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecHealthChecksItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecHealthChecksItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationSpecHealthChecksItem", Builder]:
        return GenericListBuilder[
            KustomizationSpecHealthChecksItem, KustomizationSpecHealthChecksItem.Builder
        ]()

    api_version: Annotated[str | None, Field(alias="apiVersion")] = None
    "API version of the referent, if not specified the Kubernetes preferred version will be used."
    kind: str
    "Kind of the referent."
    name: str
    "Name of the referent."
    namespace: str | None = None
    "Namespace of the referent, when not specified it acts as LocalObjectReference."


class KustomizationSpecImagesItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecImagesItem"]:
            return KustomizationSpecImagesItem

        def build(self) -> "KustomizationSpecImagesItem":
            return KustomizationSpecImagesItem(**self._attrs)

        def digest(self, value: str | None, /) -> Self:
            "Digest is the value used to replace the original image tag.\nIf digest is present NewTag value is ignored."
            return self._set("digest", value)

        def name(self, value: str, /) -> Self:
            "Name is a tag-less image name."
            return self._set("name", value)

        def new_name(self, value: str | None, /) -> Self:
            "NewName is the value used to replace the original name."
            return self._set("new_name", value)

        def new_tag(self, value: str | None, /) -> Self:
            "NewTag is the value used to replace the original tag."
            return self._set("new_tag", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecImagesItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecImagesItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecImagesItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["KustomizationSpecImagesItem", Builder]:
        return GenericListBuilder[
            KustomizationSpecImagesItem, KustomizationSpecImagesItem.Builder
        ]()

    digest: str | None = None
    "Digest is the value used to replace the original image tag.\nIf digest is present NewTag value is ignored."
    name: str
    "Name is a tag-less image name."
    new_name: Annotated[str | None, Field(alias="newName")] = None
    "NewName is the value used to replace the original name."
    new_tag: Annotated[str | None, Field(alias="newTag")] = None
    "NewTag is the value used to replace the original tag."


class KustomizationSpecKubeConfigSecretRef(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecKubeConfigSecretRef"]:
            return KustomizationSpecKubeConfigSecretRef

        def build(self) -> "KustomizationSpecKubeConfigSecretRef":
            return KustomizationSpecKubeConfigSecretRef(**self._attrs)

        def key(self, value: str | None, /) -> Self:
            "Key in the Secret, when not specified an implementation-specific default key is used."
            return self._set("key", value)

        def name(self, value: str, /) -> Self:
            "Name of the Secret."
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecKubeConfigSecretRef.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecKubeConfigSecretRef.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecKubeConfigSecretRef."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationSpecKubeConfigSecretRef", Builder]:
        return GenericListBuilder[
            KustomizationSpecKubeConfigSecretRef,
            KustomizationSpecKubeConfigSecretRef.Builder,
        ]()

    key: str | None = None
    "Key in the Secret, when not specified an implementation-specific default key is used."
    name: str
    "Name of the Secret."


class KustomizationSpecPatchesItemTarget(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecPatchesItemTarget"]:
            return KustomizationSpecPatchesItemTarget

        def build(self) -> "KustomizationSpecPatchesItemTarget":
            return KustomizationSpecPatchesItemTarget(**self._attrs)

        def annotation_selector(self, value: str | None, /) -> Self:
            "AnnotationSelector is a string that follows the label selection expression\nhttps://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api\nIt matches with the resource annotations."
            return self._set("annotation_selector", value)

        def group(self, value: str | None, /) -> Self:
            "Group is the API group to select resources from.\nTogether with Version and Kind it is capable of unambiguously identifying and/or selecting resources.\nhttps://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md"
            return self._set("group", value)

        def kind(self, value: str | None, /) -> Self:
            "Kind of the API Group to select resources from.\nTogether with Group and Version it is capable of unambiguously\nidentifying and/or selecting resources.\nhttps://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md"
            return self._set("kind", value)

        def label_selector(self, value: str | None, /) -> Self:
            "LabelSelector is a string that follows the label selection expression\nhttps://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api\nIt matches with the resource labels."
            return self._set("label_selector", value)

        def name(self, value: str | None, /) -> Self:
            "Name to match resources with."
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "Namespace to select resources from."
            return self._set("namespace", value)

        def version(self, value: str | None, /) -> Self:
            "Version of the API Group to select resources from.\nTogether with Group and Kind it is capable of unambiguously identifying and/or selecting resources.\nhttps://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md"
            return self._set("version", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecPatchesItemTarget.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecPatchesItemTarget.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecPatchesItemTarget."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationSpecPatchesItemTarget", Builder]:
        return GenericListBuilder[
            KustomizationSpecPatchesItemTarget,
            KustomizationSpecPatchesItemTarget.Builder,
        ]()

    annotation_selector: Annotated[str | None, Field(alias="annotationSelector")] = None
    "AnnotationSelector is a string that follows the label selection expression\nhttps://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api\nIt matches with the resource annotations."
    group: str | None = None
    "Group is the API group to select resources from.\nTogether with Version and Kind it is capable of unambiguously identifying and/or selecting resources.\nhttps://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md"
    kind: str | None = None
    "Kind of the API Group to select resources from.\nTogether with Group and Version it is capable of unambiguously\nidentifying and/or selecting resources.\nhttps://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md"
    label_selector: Annotated[str | None, Field(alias="labelSelector")] = None
    "LabelSelector is a string that follows the label selection expression\nhttps://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api\nIt matches with the resource labels."
    name: str | None = None
    "Name to match resources with."
    namespace: str | None = None
    "Namespace to select resources from."
    version: str | None = None
    "Version of the API Group to select resources from.\nTogether with Group and Kind it is capable of unambiguously identifying and/or selecting resources.\nhttps://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md"


class KustomizationSpecPatchesJson6902ItemPatchItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecPatchesJson6902ItemPatchItem"]:
            return KustomizationSpecPatchesJson6902ItemPatchItem

        def build(self) -> "KustomizationSpecPatchesJson6902ItemPatchItem":
            return KustomizationSpecPatchesJson6902ItemPatchItem(**self._attrs)

        def from_(self, value: str | None, /) -> Self:
            "From contains a JSON-pointer value that references a location within the target document where the operation is\nperformed. The meaning of the value depends on the value of Op, and is NOT taken into account by all operations."
            return self._set("from_", value)

        def op(self, value: Literal["test", "remove", "add", "replace", "move", "copy"], /) -> Self:
            'Op indicates the operation to perform. Its value MUST be one of "add", "remove", "replace", "move", "copy", or\n"test".\nhttps://datatracker.ietf.org/doc/html/rfc6902#section-4'
            return self._set("op", value)

        def path(self, value: str, /) -> Self:
            "Path contains the JSON-pointer value that references a location within the target document where the operation\nis performed. The meaning of the value depends on the value of Op."
            return self._set("path", value)

        def value(self, value: dict[str, Any] | None, /) -> Self:
            "Value contains a valid JSON structure. The meaning of the value depends on the value of Op, and is NOT taken into\naccount by all operations."
            return self._set("value", value)

    class BuilderContext(
        BuilderContextBase["KustomizationSpecPatchesJson6902ItemPatchItem.Builder"]
    ):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecPatchesJson6902ItemPatchItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecPatchesJson6902ItemPatchItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationSpecPatchesJson6902ItemPatchItem", Builder]:
        return GenericListBuilder[
            KustomizationSpecPatchesJson6902ItemPatchItem,
            KustomizationSpecPatchesJson6902ItemPatchItem.Builder,
        ]()

    from_: Annotated[str | None, Field(alias="from")] = None
    "From contains a JSON-pointer value that references a location within the target document where the operation is\nperformed. The meaning of the value depends on the value of Op, and is NOT taken into account by all operations."
    op: Literal["test", "remove", "add", "replace", "move", "copy"]
    'Op indicates the operation to perform. Its value MUST be one of "add", "remove", "replace", "move", "copy", or\n"test".\nhttps://datatracker.ietf.org/doc/html/rfc6902#section-4'
    path: str
    "Path contains the JSON-pointer value that references a location within the target document where the operation\nis performed. The meaning of the value depends on the value of Op."
    value: dict[str, Any] | None = None
    "Value contains a valid JSON structure. The meaning of the value depends on the value of Op, and is NOT taken into\naccount by all operations."


class KustomizationSpecPatchesJson6902ItemTarget(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecPatchesJson6902ItemTarget"]:
            return KustomizationSpecPatchesJson6902ItemTarget

        def build(self) -> "KustomizationSpecPatchesJson6902ItemTarget":
            return KustomizationSpecPatchesJson6902ItemTarget(**self._attrs)

        def annotation_selector(self, value: str | None, /) -> Self:
            "AnnotationSelector is a string that follows the label selection expression\nhttps://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api\nIt matches with the resource annotations."
            return self._set("annotation_selector", value)

        def group(self, value: str | None, /) -> Self:
            "Group is the API group to select resources from.\nTogether with Version and Kind it is capable of unambiguously identifying and/or selecting resources.\nhttps://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md"
            return self._set("group", value)

        def kind(self, value: str | None, /) -> Self:
            "Kind of the API Group to select resources from.\nTogether with Group and Version it is capable of unambiguously\nidentifying and/or selecting resources.\nhttps://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md"
            return self._set("kind", value)

        def label_selector(self, value: str | None, /) -> Self:
            "LabelSelector is a string that follows the label selection expression\nhttps://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api\nIt matches with the resource labels."
            return self._set("label_selector", value)

        def name(self, value: str | None, /) -> Self:
            "Name to match resources with."
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "Namespace to select resources from."
            return self._set("namespace", value)

        def version(self, value: str | None, /) -> Self:
            "Version of the API Group to select resources from.\nTogether with Group and Kind it is capable of unambiguously identifying and/or selecting resources.\nhttps://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md"
            return self._set("version", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecPatchesJson6902ItemTarget.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecPatchesJson6902ItemTarget.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecPatchesJson6902ItemTarget."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationSpecPatchesJson6902ItemTarget", Builder]:
        return GenericListBuilder[
            KustomizationSpecPatchesJson6902ItemTarget,
            KustomizationSpecPatchesJson6902ItemTarget.Builder,
        ]()

    annotation_selector: Annotated[str | None, Field(alias="annotationSelector")] = None
    "AnnotationSelector is a string that follows the label selection expression\nhttps://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api\nIt matches with the resource annotations."
    group: str | None = None
    "Group is the API group to select resources from.\nTogether with Version and Kind it is capable of unambiguously identifying and/or selecting resources.\nhttps://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md"
    kind: str | None = None
    "Kind of the API Group to select resources from.\nTogether with Group and Version it is capable of unambiguously\nidentifying and/or selecting resources.\nhttps://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md"
    label_selector: Annotated[str | None, Field(alias="labelSelector")] = None
    "LabelSelector is a string that follows the label selection expression\nhttps://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api\nIt matches with the resource labels."
    name: str | None = None
    "Name to match resources with."
    namespace: str | None = None
    "Namespace to select resources from."
    version: str | None = None
    "Version of the API Group to select resources from.\nTogether with Group and Kind it is capable of unambiguously identifying and/or selecting resources.\nhttps://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md"


class KustomizationSpecPostBuildSubstituteFromItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecPostBuildSubstituteFromItem"]:
            return KustomizationSpecPostBuildSubstituteFromItem

        def build(self) -> "KustomizationSpecPostBuildSubstituteFromItem":
            return KustomizationSpecPostBuildSubstituteFromItem(**self._attrs)

        def kind(self, value: Literal["Secret", "ConfigMap"], /) -> Self:
            "Kind of the values referent, valid values are (\u0027Secret\u0027, \u0027ConfigMap\u0027)."
            return self._set("kind", value)

        def name(self, value: str, /) -> Self:
            "Name of the values referent. Should reside in the same namespace as the\nreferring resource."
            return self._set("name", value)

        def optional(self, value: bool | None, /) -> Self:
            "Optional indicates whether the referenced resource must exist, or whether to\ntolerate its absence. If true and the referenced resource is absent, proceed\nas if the resource was present but empty, without any variables defined."
            return self._set("optional", value)

    class BuilderContext(
        BuilderContextBase["KustomizationSpecPostBuildSubstituteFromItem.Builder"]
    ):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecPostBuildSubstituteFromItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecPostBuildSubstituteFromItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationSpecPostBuildSubstituteFromItem", Builder]:
        return GenericListBuilder[
            KustomizationSpecPostBuildSubstituteFromItem,
            KustomizationSpecPostBuildSubstituteFromItem.Builder,
        ]()

    kind: Literal["Secret", "ConfigMap"]
    "Kind of the values referent, valid values are (\u0027Secret\u0027, \u0027ConfigMap\u0027)."
    name: Annotated[str, Field(max_length=253, min_length=1)]
    "Name of the values referent. Should reside in the same namespace as the\nreferring resource."
    optional: bool | None = False
    "Optional indicates whether the referenced resource must exist, or whether to\ntolerate its absence. If true and the referenced resource is absent, proceed\nas if the resource was present but empty, without any variables defined."


class KustomizationSpecSourceRef(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecSourceRef"]:
            return KustomizationSpecSourceRef

        def build(self) -> "KustomizationSpecSourceRef":
            return KustomizationSpecSourceRef(**self._attrs)

        def api_version(self, value: str | None, /) -> Self:
            "API version of the referent."
            return self._set("api_version", value)

        def kind(self, value: Literal["OCIRepository", "GitRepository", "Bucket"], /) -> Self:
            "Kind of the referent."
            return self._set("kind", value)

        def name(self, value: str, /) -> Self:
            "Name of the referent."
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "Namespace of the referent, defaults to the namespace of the Kubernetes resource object that contains the reference."
            return self._set("namespace", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecSourceRef.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecSourceRef.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecSourceRef."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["KustomizationSpecSourceRef", Builder]:
        return GenericListBuilder[KustomizationSpecSourceRef, KustomizationSpecSourceRef.Builder]()

    api_version: Annotated[str | None, Field(alias="apiVersion")] = None
    "API version of the referent."
    kind: Literal["OCIRepository", "GitRepository", "Bucket"]
    "Kind of the referent."
    name: str
    "Name of the referent."
    namespace: str | None = None
    "Namespace of the referent, defaults to the namespace of the Kubernetes resource object that contains the reference."


class KustomizationStatusConditionsItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationStatusConditionsItem"]:
            return KustomizationStatusConditionsItem

        def build(self) -> "KustomizationStatusConditionsItem":
            return KustomizationStatusConditionsItem(**self._attrs)

        def last_transition_time(self, value: AwareDatetime, /) -> Self:
            "lastTransitionTime is the last time the condition transitioned from one status to another.\nThis should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable."
            return self._set("last_transition_time", value)

        def message(self, value: str, /) -> Self:
            "message is a human readable message indicating details about the transition.\nThis may be an empty string."
            return self._set("message", value)

        def observed_generation(self, value: int | None, /) -> Self:
            "observedGeneration represents the .metadata.generation that the condition was set based upon.\nFor instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date\nwith respect to the current state of the instance."
            return self._set("observed_generation", value)

        def reason(self, value: str, /) -> Self:
            "reason contains a programmatic identifier indicating the reason for the condition\u0027s last transition.\nProducers of specific condition types may define expected values and meanings for this field,\nand whether the values are considered a guaranteed API.\nThe value should be a CamelCase string.\nThis field may not be empty."
            return self._set("reason", value)

        def status(self, value: Literal["True", "False", "Unknown"], /) -> Self:
            "status of the condition, one of True, False, Unknown."
            return self._set("status", value)

        def type(self, value: str, /) -> Self:
            "type of condition in CamelCase or in foo.example.com/CamelCase."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["KustomizationStatusConditionsItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationStatusConditionsItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationStatusConditionsItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationStatusConditionsItem", Builder]:
        return GenericListBuilder[
            KustomizationStatusConditionsItem, KustomizationStatusConditionsItem.Builder
        ]()

    last_transition_time: Annotated[AwareDatetime, Field(alias="lastTransitionTime")]
    "lastTransitionTime is the last time the condition transitioned from one status to another.\nThis should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable."
    message: Annotated[str, Field(max_length=32768)]
    "message is a human readable message indicating details about the transition.\nThis may be an empty string."
    observed_generation: Annotated[int | None, Field(alias="observedGeneration", ge=0)] = None
    "observedGeneration represents the .metadata.generation that the condition was set based upon.\nFor instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date\nwith respect to the current state of the instance."
    reason: Annotated[
        str,
        Field(
            max_length=1024,
            min_length=1,
            pattern="^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$",
        ),
    ]
    "reason contains a programmatic identifier indicating the reason for the condition\u0027s last transition.\nProducers of specific condition types may define expected values and meanings for this field,\nand whether the values are considered a guaranteed API.\nThe value should be a CamelCase string.\nThis field may not be empty."
    status: Literal["True", "False", "Unknown"]
    "status of the condition, one of True, False, Unknown."
    type: Annotated[
        str,
        Field(
            max_length=316,
            pattern="^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$",
        ),
    ]
    "type of condition in CamelCase or in foo.example.com/CamelCase."


class KustomizationStatusInventoryEntriesItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationStatusInventoryEntriesItem"]:
            return KustomizationStatusInventoryEntriesItem

        def build(self) -> "KustomizationStatusInventoryEntriesItem":
            return KustomizationStatusInventoryEntriesItem(**self._attrs)

        def id(self, value: str, /) -> Self:
            "ID is the string representation of the Kubernetes resource object\u0027s metadata,\nin the format \u0027\u003cnamespace\u003e_\u003cname\u003e_\u003cgroup\u003e_\u003ckind\u003e\u0027."
            return self._set("id", value)

        def v(self, value: str, /) -> Self:
            "Version is the API version of the Kubernetes resource object\u0027s kind."
            return self._set("v", value)

    class BuilderContext(BuilderContextBase["KustomizationStatusInventoryEntriesItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationStatusInventoryEntriesItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationStatusInventoryEntriesItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationStatusInventoryEntriesItem", Builder]:
        return GenericListBuilder[
            KustomizationStatusInventoryEntriesItem,
            KustomizationStatusInventoryEntriesItem.Builder,
        ]()

    id: str
    "ID is the string representation of the Kubernetes resource object\u0027s metadata,\nin the format \u0027\u003cnamespace\u003e_\u003cname\u003e_\u003cgroup\u003e_\u003ckind\u003e\u0027."
    v: str
    "Version is the API version of the Kubernetes resource object\u0027s kind."


class KustomizationSpecDecryption(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecDecryption"]:
            return KustomizationSpecDecryption

        def build(self) -> "KustomizationSpecDecryption":
            return KustomizationSpecDecryption(**self._attrs)

        def provider(self, value: Literal["sops"], /) -> Self:
            "Provider is the name of the decryption engine."
            return self._set("provider", value)

        @overload
        def secret_ref(
            self, value_or_callback: KustomizationSpecDecryptionSecretRef | None, /
        ) -> "KustomizationSpecDecryption.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [KustomizationSpecDecryptionSecretRef.Builder],
                KustomizationSpecDecryptionSecretRef.Builder | KustomizationSpecDecryptionSecretRef,
            ],
            /,
        ) -> "KustomizationSpecDecryption.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "KustomizationSpecDecryptionSecretRef.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "The secret name containing the private OpenPGP keys used for decryption."
            if self._in_context and value_or_callback is UNSET:
                context = KustomizationSpecDecryptionSecretRef.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecDecryptionSecretRef.builder())
                if isinstance(output, KustomizationSpecDecryptionSecretRef.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecDecryption.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecDecryption.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecDecryption."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["KustomizationSpecDecryption", Builder]:
        return GenericListBuilder[
            KustomizationSpecDecryption, KustomizationSpecDecryption.Builder
        ]()

    provider: Literal["sops"]
    "Provider is the name of the decryption engine."
    secret_ref: Annotated[KustomizationSpecDecryptionSecretRef | None, Field(alias="secretRef")] = (
        None
    )
    "The secret name containing the private OpenPGP keys used for decryption."


class KustomizationSpecKubeConfig(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecKubeConfig"]:
            return KustomizationSpecKubeConfig

        def build(self) -> "KustomizationSpecKubeConfig":
            return KustomizationSpecKubeConfig(**self._attrs)

        @overload
        def secret_ref(
            self, value_or_callback: KustomizationSpecKubeConfigSecretRef, /
        ) -> "KustomizationSpecKubeConfig.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [KustomizationSpecKubeConfigSecretRef.Builder],
                KustomizationSpecKubeConfigSecretRef.Builder | KustomizationSpecKubeConfigSecretRef,
            ],
            /,
        ) -> "KustomizationSpecKubeConfig.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "KustomizationSpecKubeConfigSecretRef.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "SecretRef holds the name of a secret that contains a key with\nthe kubeconfig file as the value. If no key is set, the key will default\nto \u0027value\u0027.\nIt is recommended that the kubeconfig is self-contained, and the secret\nis regularly updated if credentials such as a cloud-access-token expire.\nCloud specific `cmd-path` auth helpers will not function without adding\nbinaries and credentials to the Pod that is responsible for reconciling\nKubernetes resources."
            if self._in_context and value_or_callback is UNSET:
                context = KustomizationSpecKubeConfigSecretRef.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecKubeConfigSecretRef.builder())
                if isinstance(output, KustomizationSpecKubeConfigSecretRef.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecKubeConfig.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecKubeConfig.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecKubeConfig."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["KustomizationSpecKubeConfig", Builder]:
        return GenericListBuilder[
            KustomizationSpecKubeConfig, KustomizationSpecKubeConfig.Builder
        ]()

    secret_ref: Annotated[KustomizationSpecKubeConfigSecretRef, Field(alias="secretRef")]
    "SecretRef holds the name of a secret that contains a key with\nthe kubeconfig file as the value. If no key is set, the key will default\nto \u0027value\u0027.\nIt is recommended that the kubeconfig is self-contained, and the secret\nis regularly updated if credentials such as a cloud-access-token expire.\nCloud specific `cmd-path` auth helpers will not function without adding\nbinaries and credentials to the Pod that is responsible for reconciling\nKubernetes resources."


class KustomizationSpecPatchesItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecPatchesItem"]:
            return KustomizationSpecPatchesItem

        def build(self) -> "KustomizationSpecPatchesItem":
            return KustomizationSpecPatchesItem(**self._attrs)

        def patch_(self, value: str, /) -> Self:
            "Patch contains an inline StrategicMerge patch or an inline JSON6902 patch with\nan array of operation objects."
            return self._set("patch_", value)

        @overload
        def target(
            self, value_or_callback: KustomizationSpecPatchesItemTarget | None, /
        ) -> "KustomizationSpecPatchesItem.Builder": ...

        @overload
        def target(
            self,
            value_or_callback: Callable[
                [KustomizationSpecPatchesItemTarget.Builder],
                KustomizationSpecPatchesItemTarget.Builder | KustomizationSpecPatchesItemTarget,
            ],
            /,
        ) -> "KustomizationSpecPatchesItem.Builder": ...

        @overload
        def target(
            self, value_or_callback: Never = ...
        ) -> "KustomizationSpecPatchesItemTarget.BuilderContext": ...

        def target(self, value_or_callback=UNSET, /):
            "Target points to the resources that the patch document should be applied to."
            if self._in_context and value_or_callback is UNSET:
                context = KustomizationSpecPatchesItemTarget.BuilderContext()
                context._parent_builder = self
                context._field_name = "target"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecPatchesItemTarget.builder())
                if isinstance(output, KustomizationSpecPatchesItemTarget.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("target", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecPatchesItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecPatchesItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecPatchesItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationSpecPatchesItem", Builder]:
        return GenericListBuilder[
            KustomizationSpecPatchesItem, KustomizationSpecPatchesItem.Builder
        ]()

    patch_: Annotated[str, Field(alias="patch")]
    "Patch contains an inline StrategicMerge patch or an inline JSON6902 patch with\nan array of operation objects."
    target: KustomizationSpecPatchesItemTarget | None = None
    "Target points to the resources that the patch document should be applied to."


class KustomizationSpecPatchesJson6902Item(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecPatchesJson6902Item"]:
            return KustomizationSpecPatchesJson6902Item

        def build(self) -> "KustomizationSpecPatchesJson6902Item":
            return KustomizationSpecPatchesJson6902Item(**self._attrs)

        @overload
        def patch_(
            self,
            value_or_callback: list[KustomizationSpecPatchesJson6902ItemPatchItem],
            /,
        ) -> "KustomizationSpecPatchesJson6902Item.Builder": ...

        @overload
        def patch_(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        KustomizationSpecPatchesJson6902ItemPatchItem,
                        KustomizationSpecPatchesJson6902ItemPatchItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    KustomizationSpecPatchesJson6902ItemPatchItem,
                    KustomizationSpecPatchesJson6902ItemPatchItem.Builder,
                ]
                | list[KustomizationSpecPatchesJson6902ItemPatchItem],
            ],
            /,
        ) -> "KustomizationSpecPatchesJson6902Item.Builder": ...

        @overload
        def patch_(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[KustomizationSpecPatchesJson6902ItemPatchItem.Builder]: ...

        def patch_(self, value_or_callback=UNSET, /):
            "Patch contains the JSON6902 patch document with an array of operation objects."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[
                    KustomizationSpecPatchesJson6902ItemPatchItem.Builder
                ]()
                context._parent_builder = self
                context._field_name = "patch_"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(
                    KustomizationSpecPatchesJson6902ItemPatchItem.list_builder()
                )
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("patch_", value)

        @overload
        def target(
            self, value_or_callback: KustomizationSpecPatchesJson6902ItemTarget, /
        ) -> "KustomizationSpecPatchesJson6902Item.Builder": ...

        @overload
        def target(
            self,
            value_or_callback: Callable[
                [KustomizationSpecPatchesJson6902ItemTarget.Builder],
                KustomizationSpecPatchesJson6902ItemTarget.Builder
                | KustomizationSpecPatchesJson6902ItemTarget,
            ],
            /,
        ) -> "KustomizationSpecPatchesJson6902Item.Builder": ...

        @overload
        def target(
            self, value_or_callback: Never = ...
        ) -> "KustomizationSpecPatchesJson6902ItemTarget.BuilderContext": ...

        def target(self, value_or_callback=UNSET, /):
            "Target points to the resources that the patch document should be applied to."
            if self._in_context and value_or_callback is UNSET:
                context = KustomizationSpecPatchesJson6902ItemTarget.BuilderContext()
                context._parent_builder = self
                context._field_name = "target"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecPatchesJson6902ItemTarget.builder())
                if isinstance(output, KustomizationSpecPatchesJson6902ItemTarget.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("target", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecPatchesJson6902Item.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecPatchesJson6902Item.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecPatchesJson6902Item."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationSpecPatchesJson6902Item", Builder]:
        return GenericListBuilder[
            KustomizationSpecPatchesJson6902Item,
            KustomizationSpecPatchesJson6902Item.Builder,
        ]()

    patch_: Annotated[list[KustomizationSpecPatchesJson6902ItemPatchItem], Field(alias="patch")]
    "Patch contains the JSON6902 patch document with an array of operation objects."
    target: KustomizationSpecPatchesJson6902ItemTarget
    "Target points to the resources that the patch document should be applied to."


class KustomizationSpecPostBuild(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpecPostBuild"]:
            return KustomizationSpecPostBuild

        def build(self) -> "KustomizationSpecPostBuild":
            return KustomizationSpecPostBuild(**self._attrs)

        def substitute(self, value: dict[str, str] | None, /) -> Self:
            "Substitute holds a map of key/value pairs.\nThe variables defined in your YAML manifests\nthat match any of the keys defined in the map\nwill be substituted with the set value.\nIncludes support for bash string replacement functions\ne.g. ${var:=default}, ${var:position} and ${var/substring/replacement}."
            return self._set("substitute", value)

        @overload
        def substitute_from(
            self,
            value_or_callback: (list[KustomizationSpecPostBuildSubstituteFromItem] | None),
            /,
        ) -> "KustomizationSpecPostBuild.Builder": ...

        @overload
        def substitute_from(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        KustomizationSpecPostBuildSubstituteFromItem,
                        KustomizationSpecPostBuildSubstituteFromItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    KustomizationSpecPostBuildSubstituteFromItem,
                    KustomizationSpecPostBuildSubstituteFromItem.Builder,
                ]
                | list[KustomizationSpecPostBuildSubstituteFromItem],
            ],
            /,
        ) -> "KustomizationSpecPostBuild.Builder": ...

        @overload
        def substitute_from(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[KustomizationSpecPostBuildSubstituteFromItem.Builder]: ...

        def substitute_from(self, value_or_callback=UNSET, /):
            "SubstituteFrom holds references to ConfigMaps and Secrets containing\nthe variables and their values to be substituted in the YAML manifests.\nThe ConfigMap and the Secret data keys represent the var names and they\nmust match the vars declared in the manifests for the substitution to happen."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[KustomizationSpecPostBuildSubstituteFromItem.Builder]()
                context._parent_builder = self
                context._field_name = "substitute_from"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(
                    KustomizationSpecPostBuildSubstituteFromItem.list_builder()
                )
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("substitute_from", value)

    class BuilderContext(BuilderContextBase["KustomizationSpecPostBuild.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpecPostBuild.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpecPostBuild."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["KustomizationSpecPostBuild", Builder]:
        return GenericListBuilder[KustomizationSpecPostBuild, KustomizationSpecPostBuild.Builder]()

    substitute: dict[str, str] | None = None
    "Substitute holds a map of key/value pairs.\nThe variables defined in your YAML manifests\nthat match any of the keys defined in the map\nwill be substituted with the set value.\nIncludes support for bash string replacement functions\ne.g. ${var:=default}, ${var:position} and ${var/substring/replacement}."
    substitute_from: Annotated[
        list[KustomizationSpecPostBuildSubstituteFromItem] | None,
        Field(alias="substituteFrom"),
    ] = None
    "SubstituteFrom holds references to ConfigMaps and Secrets containing\nthe variables and their values to be substituted in the YAML manifests.\nThe ConfigMap and the Secret data keys represent the var names and they\nmust match the vars declared in the manifests for the substitution to happen."


class KustomizationStatusInventory(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationStatusInventory"]:
            return KustomizationStatusInventory

        def build(self) -> "KustomizationStatusInventory":
            return KustomizationStatusInventory(**self._attrs)

        @overload
        def entries(
            self, value_or_callback: list[KustomizationStatusInventoryEntriesItem], /
        ) -> "KustomizationStatusInventory.Builder": ...

        @overload
        def entries(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        KustomizationStatusInventoryEntriesItem,
                        KustomizationStatusInventoryEntriesItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    KustomizationStatusInventoryEntriesItem,
                    KustomizationStatusInventoryEntriesItem.Builder,
                ]
                | list[KustomizationStatusInventoryEntriesItem],
            ],
            /,
        ) -> "KustomizationStatusInventory.Builder": ...

        @overload
        def entries(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[KustomizationStatusInventoryEntriesItem.Builder]: ...

        def entries(self, value_or_callback=UNSET, /):
            "Entries of Kubernetes resource object references."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[KustomizationStatusInventoryEntriesItem.Builder]()
                context._parent_builder = self
                context._field_name = "entries"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationStatusInventoryEntriesItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("entries", value)

    class BuilderContext(BuilderContextBase["KustomizationStatusInventory.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationStatusInventory.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationStatusInventory."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["KustomizationStatusInventory", Builder]:
        return GenericListBuilder[
            KustomizationStatusInventory, KustomizationStatusInventory.Builder
        ]()

    entries: list[KustomizationStatusInventoryEntriesItem]
    "Entries of Kubernetes resource object references."


class KustomizationSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationSpec"]:
            return KustomizationSpec

        def build(self) -> "KustomizationSpec":
            return KustomizationSpec(**self._attrs)

        @overload
        def common_metadata(
            self, value_or_callback: KustomizationSpecCommonMetadata | None, /
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def common_metadata(
            self,
            value_or_callback: Callable[
                [KustomizationSpecCommonMetadata.Builder],
                KustomizationSpecCommonMetadata.Builder | KustomizationSpecCommonMetadata,
            ],
            /,
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def common_metadata(
            self, value_or_callback: Never = ...
        ) -> "KustomizationSpecCommonMetadata.BuilderContext": ...

        def common_metadata(self, value_or_callback=UNSET, /):
            "CommonMetadata specifies the common labels and annotations that are applied to all resources.\nAny existing label or annotation will be overridden if its key matches a common one."
            if self._in_context and value_or_callback is UNSET:
                context = KustomizationSpecCommonMetadata.BuilderContext()
                context._parent_builder = self
                context._field_name = "common_metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecCommonMetadata.builder())
                if isinstance(output, KustomizationSpecCommonMetadata.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("common_metadata", value)

        def components(self, value: list[str] | None, /) -> Self:
            "Components specifies relative paths to specifications of other Components."
            return self._set("components", value)

        @overload
        def decryption(
            self, value_or_callback: KustomizationSpecDecryption | None, /
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def decryption(
            self,
            value_or_callback: Callable[
                [KustomizationSpecDecryption.Builder],
                KustomizationSpecDecryption.Builder | KustomizationSpecDecryption,
            ],
            /,
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def decryption(
            self, value_or_callback: Never = ...
        ) -> "KustomizationSpecDecryption.BuilderContext": ...

        def decryption(self, value_or_callback=UNSET, /):
            "Decrypt Kubernetes secrets before applying them on the cluster."
            if self._in_context and value_or_callback is UNSET:
                context = KustomizationSpecDecryption.BuilderContext()
                context._parent_builder = self
                context._field_name = "decryption"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecDecryption.builder())
                if isinstance(output, KustomizationSpecDecryption.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("decryption", value)

        @overload
        def depends_on(
            self, value_or_callback: list[KustomizationSpecDependsOnItem] | None, /
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def depends_on(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        KustomizationSpecDependsOnItem,
                        KustomizationSpecDependsOnItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    KustomizationSpecDependsOnItem,
                    KustomizationSpecDependsOnItem.Builder,
                ]
                | list[KustomizationSpecDependsOnItem],
            ],
            /,
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def depends_on(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[KustomizationSpecDependsOnItem.Builder]: ...

        def depends_on(self, value_or_callback=UNSET, /):
            "DependsOn may contain a meta.NamespacedObjectReference slice\nwith references to Kustomization resources that must be ready before this\nKustomization can be reconciled."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[KustomizationSpecDependsOnItem.Builder]()
                context._parent_builder = self
                context._field_name = "depends_on"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecDependsOnItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("depends_on", value)

        def force(self, value: bool | None, /) -> Self:
            "Force instructs the controller to recreate resources\nwhen patching fails due to an immutable field change."
            return self._set("force", value)

        @overload
        def health_checks(
            self, value_or_callback: list[KustomizationSpecHealthChecksItem] | None, /
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def health_checks(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        KustomizationSpecHealthChecksItem,
                        KustomizationSpecHealthChecksItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    KustomizationSpecHealthChecksItem,
                    KustomizationSpecHealthChecksItem.Builder,
                ]
                | list[KustomizationSpecHealthChecksItem],
            ],
            /,
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def health_checks(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[KustomizationSpecHealthChecksItem.Builder]: ...

        def health_checks(self, value_or_callback=UNSET, /):
            "A list of resources to be included in the health assessment."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[KustomizationSpecHealthChecksItem.Builder]()
                context._parent_builder = self
                context._field_name = "health_checks"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecHealthChecksItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("health_checks", value)

        @overload
        def images(
            self, value_or_callback: list[KustomizationSpecImagesItem] | None, /
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def images(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        KustomizationSpecImagesItem, KustomizationSpecImagesItem.Builder
                    ]
                ],
                GenericListBuilder[KustomizationSpecImagesItem, KustomizationSpecImagesItem.Builder]
                | list[KustomizationSpecImagesItem],
            ],
            /,
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def images(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[KustomizationSpecImagesItem.Builder]: ...

        def images(self, value_or_callback=UNSET, /):
            "Images is a list of (image name, new name, new tag or digest)\nfor changing image names, tags or digests. This can also be achieved with a\npatch, but this operator is simpler to specify."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[KustomizationSpecImagesItem.Builder]()
                context._parent_builder = self
                context._field_name = "images"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecImagesItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("images", value)

        def interval(self, value: str, /) -> Self:
            "The interval at which to reconcile the Kustomization."
            return self._set("interval", value)

        @overload
        def kube_config(
            self, value_or_callback: KustomizationSpecKubeConfig | None, /
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def kube_config(
            self,
            value_or_callback: Callable[
                [KustomizationSpecKubeConfig.Builder],
                KustomizationSpecKubeConfig.Builder | KustomizationSpecKubeConfig,
            ],
            /,
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def kube_config(
            self, value_or_callback: Never = ...
        ) -> "KustomizationSpecKubeConfig.BuilderContext": ...

        def kube_config(self, value_or_callback=UNSET, /):
            "The KubeConfig for reconciling the Kustomization on a remote cluster.\nWhen used in combination with KustomizationSpec.ServiceAccountName,\nforces the controller to act on behalf of that Service Account at the\ntarget cluster.\nIf the --default-service-account flag is set, its value will be used as\na controller level fallback for when KustomizationSpec.ServiceAccountName\nis empty."
            if self._in_context and value_or_callback is UNSET:
                context = KustomizationSpecKubeConfig.BuilderContext()
                context._parent_builder = self
                context._field_name = "kube_config"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecKubeConfig.builder())
                if isinstance(output, KustomizationSpecKubeConfig.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("kube_config", value)

        @overload
        def patches(
            self, value_or_callback: list[KustomizationSpecPatchesItem] | None, /
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def patches(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        KustomizationSpecPatchesItem,
                        KustomizationSpecPatchesItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    KustomizationSpecPatchesItem, KustomizationSpecPatchesItem.Builder
                ]
                | list[KustomizationSpecPatchesItem],
            ],
            /,
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def patches(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[KustomizationSpecPatchesItem.Builder]: ...

        def patches(self, value_or_callback=UNSET, /):
            "Strategic merge and JSON patches, defined as inline YAML objects,\ncapable of targeting objects based on kind, label and annotation selectors."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[KustomizationSpecPatchesItem.Builder]()
                context._parent_builder = self
                context._field_name = "patches"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecPatchesItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("patches", value)

        @overload
        def patches_json6902(
            self,
            value_or_callback: list[KustomizationSpecPatchesJson6902Item] | None,
            /,
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def patches_json6902(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        KustomizationSpecPatchesJson6902Item,
                        KustomizationSpecPatchesJson6902Item.Builder,
                    ]
                ],
                GenericListBuilder[
                    KustomizationSpecPatchesJson6902Item,
                    KustomizationSpecPatchesJson6902Item.Builder,
                ]
                | list[KustomizationSpecPatchesJson6902Item],
            ],
            /,
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def patches_json6902(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[KustomizationSpecPatchesJson6902Item.Builder]: ...

        def patches_json6902(self, value_or_callback=UNSET, /):
            "JSON 6902 patches, defined as inline YAML objects.\nDeprecated: Use Patches instead."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[KustomizationSpecPatchesJson6902Item.Builder]()
                context._parent_builder = self
                context._field_name = "patches_json6902"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecPatchesJson6902Item.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("patches_json6902", value)

        def patches_strategic_merge(self, value: list[dict[str, Any]] | None, /) -> Self:
            "Strategic merge patches, defined as inline YAML objects.\nDeprecated: Use Patches instead."
            return self._set("patches_strategic_merge", value)

        def path(self, value: str | None, /) -> Self:
            "Path to the directory containing the kustomization.yaml file, or the\nset of plain YAMLs a kustomization.yaml should be generated for.\nDefaults to \u0027None\u0027, which translates to the root path of the SourceRef."
            return self._set("path", value)

        @overload
        def post_build(
            self, value_or_callback: KustomizationSpecPostBuild | None, /
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def post_build(
            self,
            value_or_callback: Callable[
                [KustomizationSpecPostBuild.Builder],
                KustomizationSpecPostBuild.Builder | KustomizationSpecPostBuild,
            ],
            /,
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def post_build(
            self, value_or_callback: Never = ...
        ) -> "KustomizationSpecPostBuild.BuilderContext": ...

        def post_build(self, value_or_callback=UNSET, /):
            "PostBuild describes which actions to perform on the YAML manifest\ngenerated by building the kustomize overlay."
            if self._in_context and value_or_callback is UNSET:
                context = KustomizationSpecPostBuild.BuilderContext()
                context._parent_builder = self
                context._field_name = "post_build"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecPostBuild.builder())
                if isinstance(output, KustomizationSpecPostBuild.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("post_build", value)

        def prune(self, value: bool, /) -> Self:
            "Prune enables garbage collection."
            return self._set("prune", value)

        def retry_interval(self, value: str | None, /) -> Self:
            "The interval at which to retry a previously failed reconciliation.\nWhen not specified, the controller uses the KustomizationSpec.Interval\nvalue to retry failures."
            return self._set("retry_interval", value)

        def service_account_name(self, value: str | None, /) -> Self:
            "The name of the Kubernetes service account to impersonate\nwhen reconciling this Kustomization."
            return self._set("service_account_name", value)

        @overload
        def source_ref(
            self, value_or_callback: KustomizationSpecSourceRef, /
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def source_ref(
            self,
            value_or_callback: Callable[
                [KustomizationSpecSourceRef.Builder],
                KustomizationSpecSourceRef.Builder | KustomizationSpecSourceRef,
            ],
            /,
        ) -> "KustomizationSpec.Builder": ...

        @overload
        def source_ref(
            self, value_or_callback: Never = ...
        ) -> "KustomizationSpecSourceRef.BuilderContext": ...

        def source_ref(self, value_or_callback=UNSET, /):
            "Reference of the source where the kustomization file is."
            if self._in_context and value_or_callback is UNSET:
                context = KustomizationSpecSourceRef.BuilderContext()
                context._parent_builder = self
                context._field_name = "source_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpecSourceRef.builder())
                if isinstance(output, KustomizationSpecSourceRef.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("source_ref", value)

        def suspend(self, value: bool | None, /) -> Self:
            "This flag tells the controller to suspend subsequent kustomize executions,\nit does not apply to already started executions. Defaults to false."
            return self._set("suspend", value)

        def target_namespace(self, value: str | None, /) -> Self:
            "TargetNamespace sets or overrides the namespace in the\nkustomization.yaml file."
            return self._set("target_namespace", value)

        def timeout(self, value: str | None, /) -> Self:
            "Timeout for validation, apply and health checking operations.\nDefaults to \u0027Interval\u0027 duration."
            return self._set("timeout", value)

        def validation(self, value: Literal["none", "client", "server"] | None, /) -> Self:
            "Deprecated: Not used in v1beta2."
            return self._set("validation", value)

        def wait(self, value: bool | None, /) -> Self:
            "Wait instructs the controller to check the health of all the reconciled resources.\nWhen enabled, the HealthChecks are ignored. Defaults to false."
            return self._set("wait", value)

    class BuilderContext(BuilderContextBase["KustomizationSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["KustomizationSpec", Builder]:
        return GenericListBuilder[KustomizationSpec, KustomizationSpec.Builder]()

    common_metadata: Annotated[
        KustomizationSpecCommonMetadata | None, Field(alias="commonMetadata")
    ] = None
    "CommonMetadata specifies the common labels and annotations that are applied to all resources.\nAny existing label or annotation will be overridden if its key matches a common one."
    components: list[str] | None = None
    "Components specifies relative paths to specifications of other Components."
    decryption: KustomizationSpecDecryption | None = None
    "Decrypt Kubernetes secrets before applying them on the cluster."
    depends_on: Annotated[list[KustomizationSpecDependsOnItem] | None, Field(alias="dependsOn")] = (
        None
    )
    "DependsOn may contain a meta.NamespacedObjectReference slice\nwith references to Kustomization resources that must be ready before this\nKustomization can be reconciled."
    force: bool | None = False
    "Force instructs the controller to recreate resources\nwhen patching fails due to an immutable field change."
    health_checks: Annotated[
        list[KustomizationSpecHealthChecksItem] | None, Field(alias="healthChecks")
    ] = None
    "A list of resources to be included in the health assessment."
    images: list[KustomizationSpecImagesItem] | None = None
    "Images is a list of (image name, new name, new tag or digest)\nfor changing image names, tags or digests. This can also be achieved with a\npatch, but this operator is simpler to specify."
    interval: Annotated[str, Field(pattern="^([0-9]+(\\.[0-9]+)?(ms|s|m|h))+$")]
    "The interval at which to reconcile the Kustomization."
    kube_config: Annotated[KustomizationSpecKubeConfig | None, Field(alias="kubeConfig")] = None
    "The KubeConfig for reconciling the Kustomization on a remote cluster.\nWhen used in combination with KustomizationSpec.ServiceAccountName,\nforces the controller to act on behalf of that Service Account at the\ntarget cluster.\nIf the --default-service-account flag is set, its value will be used as\na controller level fallback for when KustomizationSpec.ServiceAccountName\nis empty."
    patches: list[KustomizationSpecPatchesItem] | None = None
    "Strategic merge and JSON patches, defined as inline YAML objects,\ncapable of targeting objects based on kind, label and annotation selectors."
    patches_json6902: Annotated[
        list[KustomizationSpecPatchesJson6902Item] | None,
        Field(alias="patchesJson6902"),
    ] = None
    "JSON 6902 patches, defined as inline YAML objects.\nDeprecated: Use Patches instead."
    patches_strategic_merge: Annotated[
        list[dict[str, Any]] | None, Field(alias="patchesStrategicMerge")
    ] = None
    "Strategic merge patches, defined as inline YAML objects.\nDeprecated: Use Patches instead."
    path: str | None = None
    "Path to the directory containing the kustomization.yaml file, or the\nset of plain YAMLs a kustomization.yaml should be generated for.\nDefaults to \u0027None\u0027, which translates to the root path of the SourceRef."
    post_build: Annotated[KustomizationSpecPostBuild | None, Field(alias="postBuild")] = None
    "PostBuild describes which actions to perform on the YAML manifest\ngenerated by building the kustomize overlay."
    prune: bool
    "Prune enables garbage collection."
    retry_interval: Annotated[
        str | None,
        Field(alias="retryInterval", pattern="^([0-9]+(\\.[0-9]+)?(ms|s|m|h))+$"),
    ] = None
    "The interval at which to retry a previously failed reconciliation.\nWhen not specified, the controller uses the KustomizationSpec.Interval\nvalue to retry failures."
    service_account_name: Annotated[str | None, Field(alias="serviceAccountName")] = None
    "The name of the Kubernetes service account to impersonate\nwhen reconciling this Kustomization."
    source_ref: Annotated[KustomizationSpecSourceRef, Field(alias="sourceRef")]
    "Reference of the source where the kustomization file is."
    suspend: bool | None = None
    "This flag tells the controller to suspend subsequent kustomize executions,\nit does not apply to already started executions. Defaults to false."
    target_namespace: Annotated[
        str | None, Field(alias="targetNamespace", max_length=63, min_length=1)
    ] = None
    "TargetNamespace sets or overrides the namespace in the\nkustomization.yaml file."
    timeout: Annotated[str | None, Field(pattern="^([0-9]+(\\.[0-9]+)?(ms|s|m|h))+$")] = None
    "Timeout for validation, apply and health checking operations.\nDefaults to \u0027Interval\u0027 duration."
    validation: Literal["none", "client", "server"] | None = None
    "Deprecated: Not used in v1beta2."
    wait: bool | None = None
    "Wait instructs the controller to check the health of all the reconciled resources.\nWhen enabled, the HealthChecks are ignored. Defaults to false."


class KustomizationStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KustomizationStatus"]:
            return KustomizationStatus

        def build(self) -> "KustomizationStatus":
            return KustomizationStatus(**self._attrs)

        @overload
        def conditions(
            self, value_or_callback: list[KustomizationStatusConditionsItem] | None, /
        ) -> "KustomizationStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        KustomizationStatusConditionsItem,
                        KustomizationStatusConditionsItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    KustomizationStatusConditionsItem,
                    KustomizationStatusConditionsItem.Builder,
                ]
                | list[KustomizationStatusConditionsItem],
            ],
            /,
        ) -> "KustomizationStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[KustomizationStatusConditionsItem.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[KustomizationStatusConditionsItem.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationStatusConditionsItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        @overload
        def inventory(
            self, value_or_callback: KustomizationStatusInventory | None, /
        ) -> "KustomizationStatus.Builder": ...

        @overload
        def inventory(
            self,
            value_or_callback: Callable[
                [KustomizationStatusInventory.Builder],
                KustomizationStatusInventory.Builder | KustomizationStatusInventory,
            ],
            /,
        ) -> "KustomizationStatus.Builder": ...

        @overload
        def inventory(
            self, value_or_callback: Never = ...
        ) -> "KustomizationStatusInventory.BuilderContext": ...

        def inventory(self, value_or_callback=UNSET, /):
            "Inventory contains the list of Kubernetes resource object references that have been successfully applied."
            if self._in_context and value_or_callback is UNSET:
                context = KustomizationStatusInventory.BuilderContext()
                context._parent_builder = self
                context._field_name = "inventory"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationStatusInventory.builder())
                if isinstance(output, KustomizationStatusInventory.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("inventory", value)

        def last_applied_revision(self, value: str | None, /) -> Self:
            "The last successfully applied revision.\nEquals the Revision of the applied Artifact from the referenced Source."
            return self._set("last_applied_revision", value)

        def last_attempted_revision(self, value: str | None, /) -> Self:
            "LastAttemptedRevision is the revision of the last reconciliation attempt."
            return self._set("last_attempted_revision", value)

        def last_handled_reconcile_at(self, value: str | None, /) -> Self:
            "LastHandledReconcileAt holds the value of the most recent\nreconcile request value, so a change of the annotation value\ncan be detected."
            return self._set("last_handled_reconcile_at", value)

        def observed_generation(self, value: int | None, /) -> Self:
            "ObservedGeneration is the last reconciled generation."
            return self._set("observed_generation", value)

    class BuilderContext(BuilderContextBase["KustomizationStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KustomizationStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KustomizationStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["KustomizationStatus", Builder]:
        return GenericListBuilder[KustomizationStatus, KustomizationStatus.Builder]()

    conditions: list[KustomizationStatusConditionsItem] | None = None
    inventory: KustomizationStatusInventory | None = None
    "Inventory contains the list of Kubernetes resource object references that have been successfully applied."
    last_applied_revision: Annotated[str | None, Field(alias="lastAppliedRevision")] = None
    "The last successfully applied revision.\nEquals the Revision of the applied Artifact from the referenced Source."
    last_attempted_revision: Annotated[str | None, Field(alias="lastAttemptedRevision")] = None
    "LastAttemptedRevision is the revision of the last reconciliation attempt."
    last_handled_reconcile_at: Annotated[str | None, Field(alias="lastHandledReconcileAt")] = None
    "LastHandledReconcileAt holds the value of the most recent\nreconcile request value, so a change of the annotation value\ncan be detected."
    observed_generation: Annotated[int | None, Field(alias="observedGeneration")] = None
    "ObservedGeneration is the last reconciled generation."


class Kustomization(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "kustomizations",
        "scope": "Namespaced",
        "status": True,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Kustomization"]:
            return Kustomization

        def build(self) -> "Kustomization":
            return Kustomization(**self._attrs)

        def api_version(self, value: Literal["kustomize.toolkit.fluxcd.io/v1beta2"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["Kustomization"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Kustomization.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Kustomization.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(
            self, value_or_callback: KustomizationSpec | None, /
        ) -> "Kustomization.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [KustomizationSpec.Builder],
                KustomizationSpec.Builder | KustomizationSpec,
            ],
            /,
        ) -> "Kustomization.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "KustomizationSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = KustomizationSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationSpec.builder())
                if isinstance(output, KustomizationSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(
            self, value_or_callback: KustomizationStatus | None, /
        ) -> "Kustomization.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [KustomizationStatus.Builder],
                KustomizationStatus.Builder | KustomizationStatus,
            ],
            /,
        ) -> "Kustomization.Builder": ...

        @overload
        def status(
            self, value_or_callback: Never = ...
        ) -> "KustomizationStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = KustomizationStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KustomizationStatus.builder())
                if isinstance(output, KustomizationStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["Kustomization.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Kustomization.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Kustomization."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Kustomization", Builder]:
        return GenericListBuilder[Kustomization, Kustomization.Builder]()

    api_version: Annotated[
        Literal["kustomize.toolkit.fluxcd.io/v1beta2"], Field(alias="apiVersion")
    ] = "kustomize.toolkit.fluxcd.io/v1beta2"
    "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["Kustomization"] = "Kustomization"
    "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    spec: KustomizationSpec | None = None
    status: KustomizationStatus | None = None
